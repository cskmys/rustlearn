fn main() {
    // addition
    let sum = 5 + 10;
    println!("sum is {}", sum);

    // subtraction
    let difference = 95.5 - 4.3;
    println!("difference is {}", difference);

    // multiplication
    let product = 4 * 30;
    println!("product is {}", product);

    // division
    let quotient = 56.7 / 32.2;
    println!("quotient is {}", quotient);

    let floored = 2 / 3; // Results in 0 as integers are divided not floats
    println!("quotient is {}", floored);

    // remainder
    let remainder = 43 % 5;
    println!("remainder is {}", remainder);

    let heart_eyed_cat = '😻';
    println!("character is {}", heart_eyed_cat);
}
