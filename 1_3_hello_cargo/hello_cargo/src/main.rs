fn main() {
    println!("Hello, world!");
}

/*
build:
cd ./hello_cargo
cargo build

run:
./target/debug/hello_cargo

build and run:
cargo run

build without generating executable(way faster than generating executable and can be used continuously while coding):
cargo check

release build:
cargo build --release
*/